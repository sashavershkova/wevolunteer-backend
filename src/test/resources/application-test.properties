# Test-only overrides. Loaded on top of application.properties when the "test"
# profile is active, so main settings such as the Cognito issuer still apply.
aws.s3.profile-images-bucket=test-profile-images-bucket
aws.sns.topic-arn=arn:test
